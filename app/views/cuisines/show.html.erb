<h1><%= @cuisine.name %></h1>

<% if @cuisine.recipes.empty? %>
  <p>Nenhuma receita encontrada para este tipo de cozinha</p>
<% end %>

<% @cuisine.recipes.each do |recipe| %>
  <%= render partial: "recipes/recipe_link", locals: {recipe: recipe} %>
<% end %>

<%= link_to t('simple_form.back'), root_path %>