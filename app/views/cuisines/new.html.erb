<h1>Cadastre uma Cozinha</h1>

<%= simple_form_for(@cuisine, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.submit t('simple_form.buttons.submit') %>
<% end %>
<%= link_to t('simple_form.back'), root_path %>